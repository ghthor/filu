package quad_test

import "github.com/ghthor/gospec"

func DescribeQuad(c gospec.Context) {
	c.Specify("a quad tree", func() {
		c.Specify("can insert an entity", func() {
		})

		c.Specify("will subdivide", func() {
		})

		c.Specify("can remove an entity", func() {
		})

		c.Specify("can be queried", func() {
			c.Specify("by cell", func() {
			})

			c.Specify("by bounds", func() {
			})
		})
	})
}
